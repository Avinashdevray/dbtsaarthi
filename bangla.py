def convert_english_digit_to_bangla_digit(text: str) -> str:
    """Stub implementation used to satisfy Coqui TTS optional dependency.

    The application does not support Bangla transcription, so this function
    simply returns the original text without modification.
    """
    return text
